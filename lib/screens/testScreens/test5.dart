import 'package:flutter/material.dart';

class TestScreen5 extends StatelessWidget {
  const TestScreen5({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('TestScreen5'),
      ),
    );
  }
}
