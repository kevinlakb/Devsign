import 'package:flutter/material.dart';

class TestScreen3 extends StatelessWidget {
  const TestScreen3({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('TestScreen3'),
      ),
    );
  }
}
