import 'package:flutter/material.dart';

class TestScreen4 extends StatelessWidget {
  const TestScreen4({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('TestScreen4'),
      ),
    );
  }
}
