import 'package:flutter/material.dart';

class TestScreen7 extends StatelessWidget {
  const TestScreen7({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('TestScreen7'),
      ),
    );
  }
}
